name: Automated fixes for Pull Requests
on:
  pull_request_target:

jobs:
  triage:
    runs-on: ubuntu-latest
    steps:
      - uses: ericcornelissen/svgo-action@v1
        with:
          repo-token: ${{ secrets.GITHUB_TOKEN }}
          svg-options: .svgo.yml
          ignore: images/*.svg
